import React from 'react'

const Formula = props => {
    return (
        <div id="formula" className="calculator__formula">
            {props.formula}
        </div>
    )
}

export default Formula
